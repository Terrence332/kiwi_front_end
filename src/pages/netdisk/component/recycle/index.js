import React from 'react'
import {Layout} from 'antd'

const { Content } = Layout


class Recycle extends React.PureComponent{
  render(){
    return(
      <Content style={{background: "#fff", padding: 24, }}>
        <div>Waiting for developing.</div>
      </Content>
    )
  }
}

export default Recycle